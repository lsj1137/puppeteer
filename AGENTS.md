- Memory 제안은 대화에서 즉시 내용과 이유를 보여주고 승인·거절·diff 검토를 선택할 수 있어야 한다. 기존 Memory 메뉴의 검토함은 유지하며 최신 대기 제안을 자동으로 연다.

- 첫 공개 Agent 예제는 `agents/release-maintainer.md`로 관리한다. 검증·버전·태그·CI 트리거 점검을 담당하며, 커밋·태그·푸시·Release 같은 외부 변경은 사용자가 명시적으로 요청한 경우에만 수행한다.

- 프롬프트 입력창은 프로젝트별 명령 기록을 유지하고 ↑/↓ 방향키로 탐색할 수 있어야 한다. 세션 실행 중에도 다음 지시 한 건을 미리 예약할 수 있으며 현재 작업 종료 후 같은 세션에 자동 전송한다.

- 자동 커밋·병합 완료 알림은 처음에는 전체 내용을 보여주고, 사용자가 대화를 스크롤하거나 입력을 시작하면 애니메이션과 함께 성공 아이콘으로 축소한다. 아이콘을 누르면 다시 펼칠 수 있다.

- Worktree 자동 반영 진단은 관리 화면에 접힌 한 줄 상태로 표시하고, 상세 영역에서 처리 시각·경로·Git 상태·실패 원인을 확인하도록 한다. 일반 대화에는 최종 성공 또는 검토 필요 안내만 남긴다.

- Worktree 작업 반영은 `자동 병합`과 `병합 제안` 두 모드를 제공하며 기본값은 자동 병합이다. 자동 모드는 정상 완료 시 변경을 커밋하고 안전한 fast-forward만 수행하며, 안전 조건을 충족하지 못하면 병합 제안으로 전환한다.

- Windows와 WSL이 같은 worktree를 사용하는 경우 Git 상태 확인·커밋·병합은 일관된 줄바꿈 정규화 기준을 사용해야 하며, 줄바꿈 차이만으로 원본을 dirty로 판단해서는 안 된다.

자동 병합 모드에서 승인된 Project Memory 변경은 정본 파일만 별도 커밋한다. 이 커밋으로 기존 worktree가 뒤처지면 무충돌 rebase 후 fast-forward 병합하며, 기존 Memory 파일 변경이나 충돌이 있으면 자동 처리하지 않고 검토로 전환한다.

사용량 영역은 기본적으로 접힌 상태에서 아이콘·레이블·프로그레스바·퍼센트만 보여주고, 펼치면 상세 사용량을 표시한다. 실행 중인 세션이 있을 때는 창을 닫기 전에 종료 여부를 확인한다.

우측 보조 패널은 Git 저장소에서만 Git 탭을 표시하고, 파일 탭에서는 프로젝트·worktree 내부 파일의 읽기 전용 미리보기를 제공한다. 입력창 위 설정 순서는 실행환경 다음 에이전트로 유지한다.

승인 요청은 실행 중인 세션에만 유효한 일회성 상태로 취급한다. 앱 재시작이나 세션 종료 시 미결 승인 내역을 즉시 폐기하고, 보류된 내역은 사용자가 직접 지울 수 있게 한다.

입력창 위의 실행환경·에이전트·자동 반영 상태는 하나의 접힌 세션 컨텍스트 바로 통합한다. 요약 버튼을 누르면 레이아웃 이동 없는 오버레이에서 설정과 최근 반영 내역을 확인한다.

입력창 위 세션 도구 영역은 전체를 접고 펼칠 수 있게 하며, 펼친 상태에서 실행환경·에이전트·반영 상태를 독립 버튼으로 제공한다. 각 버튼은 해당 정보만 담은 별도 오버레이를 연다.

입력창 위 세션 도구 모음은 펼치면 실행환경·에이전트·반영 상태를 직접 표시하고, 접으면 바 전체가 사라진 뒤 작은 펼치기 핸들만 남긴다. 각 도구는 독립 오버레이를 연다.

대화가 맨 아래에서 벗어나면 대화 영역 우측 아래에 맨 아래로 이동하는 플로팅 버튼을 표시하고, 하단에 도달하면 숨긴다.
